import { GpCompleteDirective } from './gp-complete.directive';

describe('GpCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new GpCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
